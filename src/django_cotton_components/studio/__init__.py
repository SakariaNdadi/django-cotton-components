"""No-code seam: build panels/resources from stored configuration.

Add ``"django_cotton_components.studio"`` to INSTALLED_APPS and call
``Panel(...).dynamic()`` to serve :class:`~.models.DashboardSpec` rows as
resources. See ``docs/no-code.md``.
"""
